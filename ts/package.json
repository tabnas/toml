{
  "name": "@tabnas/toml",
  "version": "0.5.6",
  "description": "This plugin allows the [Jsonic](https://github.com/tabnas/jsonic) JSON parser to support toml syntax.",
  "main": "dist/toml.js",
  "type": "commonjs",
  "types": "dist/toml.d.ts",
  "homepage": "https://github.com/tabnas/toml",
  "keywords": [
    "pattern",
    "matcher",
    "object",
    "property",
    "json"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/toml.git",
    "directory": "ts"
  },
  "scripts": {
    "pretest": "npm run build && bash ../scripts/fetch-toml-test.sh",
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "pretest-cov": "npm run build && bash ../scripts/fetch-toml-test.sh",
    "test-cov": "rm -rf ./coverage && mkdir -p ./coverage && node --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --enable-source-maps --test \"dist-test/*.test.js\"",
    "pretest-some": "npm run build && bash ../scripts/fetch-toml-test.sh",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "watch": "tsc --build src test -w",
    "embed": "node embed-grammar.js",
    "build": "node embed-grammar.js && tsc --build src test",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json test/toml-test",
    "install-toml-test": "bash ../scripts/fetch-toml-test.sh",
    "reset": "npm run clean && npm i && npm run install-toml-test && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/jsonic": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@tabnas/support": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "peerDependencies": {
    "@tabnas/jsonic": ">=0",
    "@tabnas/parser": ">=0"
  },
  "engines": {
    "node": ">=24"
  }
}
